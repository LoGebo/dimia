# ---------- Modo A: Postgres directo (desarrollo local) ----------
PG_DSN=postgresql://geboou@localhost:5432/agenda_test
AUTH_MODE=local
SESION_SECRETO=cambia-esto-por-32-bytes-aleatorios

# ---------- Modo B: Supabase gestionado ----------
# AUTH_MODE=supabase
# NEXT_PUBLIC_SUPABASE_URL=https://<ref>.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key>
# PG_DSN=postgresql://postgres.<ref>:<password>@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# ---------- Copiloto (chat con datos, en Ajustes/panel) ----------
OPENAI_API_KEY=sk-...            # requerido para el copiloto
COPILOTO_MODELO=gpt-4.1-mini     # opcional; por omisión gpt-4.1-mini

# ---------- Enlaces de pago y "Probar" desde el navegador ----------
PANEL_URL=https://panel.tu-dominio.mx   # origen público para las URLs de webhook de pago
LIVEKIT_URL=wss://<proyecto>.livekit.cloud
LIVEKIT_API_KEY=<api key>
LIVEKIT_API_SECRET=<api secret>
